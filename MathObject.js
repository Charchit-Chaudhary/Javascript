// Math object is a built-in object that has properties and methods for mathematical constants and functions. 
//properties has no parantheses because they are not functions, they are values.so they just read the values.whereas methods has parantheses because they are functions, they perform some operations and return a value.
let x=2;
let y=3;

console.log(Math.max(x,y));
// Random number between 1 and 100
console.log(Math.floor(Math.random()*100)+1);
let a=9.5;
console.log(Math.round(a));//rounds to the nearest integer
console.log(Math.floor(a)); //always rounds down

